package VirtualPetShelter;

public class VirtualPetShelter {

    String myShelter;
    int wellnessLevel;  //Take pet to Vet.
    int hungerLevel; // Feed Pet.
    int thirstLevel; //Give pet water.
    int sleepLevel; //Pet needs rest.
    int boredomLevel; // Play with pet.
    String userPetChoice;
    private int ownerResponse = 0;




    public static void main(String[] args){
        public VirtualPetShelter(String myShelter, wellnessLevel, hungerLevel, thirstLevel, sleepLevel, boredomLevel);

            String myShelter;
            int wellnessLevel;  //Take pet to Vet.
            int hungerLevel; // Feed Pet.
            int thirstLevel; //Give pet water.
            int sleepLevel; //Pet needs rest.
            int boredomLevel; // Play with pet.
            String userPetChoice;


            public void tick (); {
                hungerLevel += 1;
                wellnessLevel -= 1;
                thirstLevel += 1;
                sleepLevel += 1;
                boredomLevel += 1;
            }
        }

            //          (String boogie, int i, int i1, int i2, int i3, int i4, int i5)
            //          (String spot, int i, int i1, int i2, int i3, int i4, int i5)
            //          (String blackie, int i, int i1, int i2, int i3, int i4, int i5)
            //          (String buster, int i, int i1, int i2, int i3, int i4, int i5)  {

//                if (VirtualPetShelter.contains("fido")){
//                    System.out.println(("I have a dog"));
        }
        if (myShelter.contains("Fido")) {
            System.out.println(("I have a dog"));
        }
    }
}